# Browser / E2E test defaults — copy to `.env.test` (gitignored) to override.
# These values are local-only and must never be used on Netlify.

DATABASE_URL=postgresql://deep:deep-local-only@localhost:5432/deep?sslmode=disable
PAYLOAD_SECRET=playwright-only-secret-with-32-characters
NEXT_PUBLIC_SERVER_URL=http://127.0.0.1:3100
DEMO_CONTENT_ENABLED=true
SITE_INDEXING_ENABLED=false
